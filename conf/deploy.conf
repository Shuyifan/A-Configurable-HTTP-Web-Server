listen 80;

root /files;

handler static {
    location /static;
    root static;
}

handler static {
    location /foo;
    root static;
}

handler echo {
    location /echo;
}

handler error {
    location /;
}

handler status {
    location /status;
}

handler createForm {
    location /meme/new;
    root create_form.html;
}

handler accept {
    location /meme/create;
    root accept.html;
}

handler listMeme {
    location /meme/list;
    root meme_list.html;
}
